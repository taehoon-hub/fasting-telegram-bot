const { onRequest } = require("firebase-functions/v2/https");

exports.processReminder = onRequest((req, res) => {
  res.status(200).json({
    ok: true,
    message: "processReminder is deployed"
  });
});
